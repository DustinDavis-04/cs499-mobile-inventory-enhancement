<?xml version="1.0" encoding="utf-8"?>
<!-- Single inventory row used by the RecyclerView -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Divider between rows -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#555555" />

    <!-- Main row content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="6dp"
        android:paddingTop="8dp"
        android:paddingEnd="6dp"
        android:paddingBottom="8dp">

        <!-- Item name and low stock indicator -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Shown only when quantity is at or below reorder threshold -->
            <ImageView
                android:id="@+id/lowStockIcon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_marginEnd="6dp"
                android:contentDescription="Low stock alert"
                android:src="@android:drawable/ic_dialog_alert"
                android:visibility="gone"
                app:tint="#FF5252" />

            <!-- Item name, trimmed to one line -->
            <TextView
                android:id="@+id/itemNameText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:gravity="center_vertical"
                android:maxLines="1"
                android:minHeight="48dp"
                android:text="Item"
                android:textColor="#FFFFFF" />

        </LinearLayout>

        <!-- Current quantity is view only on the inventory screen -->
        <TextView
            android:id="@+id/qtyText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:minHeight="48dp"
            android:text="0"
            android:textColor="#FFFFFF" />

        <!-- Row actions -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingEnd="4dp">

            <!-- Opens the item details screen -->
            <Button
                android:id="@+id/infoButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@android:color/transparent"
                android:text="Info"
                android:textAllCaps="false"
                android:textColor="#2196F3"
                android:textSize="12sp" />

            <!-- Removes the item from active inventory after confirmation -->
            <ImageButton
                android:id="@+id/deleteButton"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="8dp"
                android:background="@android:color/transparent"
                android:contentDescription="Remove item"
                android:src="@android:drawable/ic_delete"
                app:tint="#D32F2F" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>